# feign Plugin Generator

For efficiency reasons the datatype and wrapper code in plugins is very
verbose. This makes implementing replay plugins for feign very work intense when following a manual approach. 

The same problem occurs when instrumenting libraries. The approach here
aims to use annotations that allow automatic generation if instrumentation
and replay related code.


